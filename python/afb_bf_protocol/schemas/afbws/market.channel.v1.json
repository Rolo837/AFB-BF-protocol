{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rolo837/AFB-BF-protocol/spec/schemas/afbws/market.channel.v1.json",
  "x-afbws-support-id": "afbws.market.channel.v1",
  "title": "market channel — schema-first (AFB backend<->AFB frontend only; not part of afb.execution.v1 wire)",
  "description": "Negotiated via auth.support/auth_ok.support (capability id afbws.market.channel.v1). Replaces the legacy `candles` channel (klines + background datasets positions/trades/hhi/orders), `stream/favorites`, `stream/positions`, and `securities/marketdata|futures|positions` for clients that negotiated this capability; legacy stays available as fallback for clients that did not — this migration carries no version-gated behaviour, negotiation is purely by `auth.support`/`auth_ok.support`. Every instrument is addressed by the catalog's full `instrument_key` (see common.v1.json#/$defs/instrumentKey), not a bare SECID. Two data templates cover the subscribable/pushable payload shapes: `series` (candles and every background dataset, keyed by `time`) and `snapshot` (a cross-instrument slice, keyed by `instrument_key`), both columnar (`$defs/table`: `columns` + `rows`). The same schema id serves both as the reply to a `get` (carries `request_id`) and as an unsolicited push to a live subscription (no `request_id`) — deliberately no `.request`/`.response`/`.push` suffix on schema ids. Every data message carries `received_at` (when AFB received the data from its source) and `tz` (the trading venue's IANA zone that row-level `time`/day boundaries are aligned to). Two independent subscription mechanisms exist, deliberately not unified: `subscribe` is explicit and replace-all for the `quotes` (favorites) and `futures` (screener) snapshot scopes — the client states its whole desired scope every time, the server does not diff against a previous statement; the series (candles/dataset) subscription has no separate message at all — a connection has at most one, and it is simply whatever `get` (target=series) was last sent with a live `end_date` (see `get`'s description). A third `get` target, `calendar`, is a plain one-shot lookup with no subscription or push counterpart: it answers with `$defs/calendar` (`afbws.market.calendar.v1`) — the trading-session windows and daily-bar dates AFB derived for one instrument from its trading-calendar state (the same state pushed to BF via afb2bf `calendar.update`, see payloads/calendar.update.json), used by the frontend to place future (whitespace) bar slots on the chart. `NaN` never appears on the wire — sources that produce it are converted to `null`. See AFB/docs/ws/market.md.",
  "type": "object",
  "oneOf": [
    { "$ref": "#/$defs/subscribe" },
    { "$ref": "#/$defs/subscription" },
    { "$ref": "#/$defs/get" },
    { "$ref": "#/$defs/series" },
    { "$ref": "#/$defs/snapshot" },
    { "$ref": "#/$defs/calendar" },
    { "$ref": "#/$defs/error" }
  ],
  "$defs": {
    "table": {
      "type": "object",
      "title": "Columnar data block for one dataset `kind`.",
      "description": "`rows` are keyed by their first column: `time` (UTC unix seconds) for the series kinds (candles/positions/trades/hhi/orders), `instrument_key` for the snapshot kinds (quote/oi/oi_daily). Beyond that first column, `columns` may list any subset of the kind's allowed fields in any order — a client reads by column name, never by positional index — so the server can add a field later without a schema bump, as long as it stays inside the per-kind enum below. `rows` elements are `number | string | null`; a source value that is NaN is sent as `null`, never as the string \"NaN\" or JSON NaN. In a `series` message, non-candle dataset rows (positions/trades/hhi/orders) exist only at the timestamps of that same message's `candles` table — the server aligns them: `positions` is snapped to the bucket `[t, next candle)` and carries the candle's `t`; `trades`/`orders`/`hhi` require an exact `time` match. There are no rows outside trading hours (no forward-filled/stale values).",
      "required": ["kind", "columns", "rows"],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "enum": ["candles", "positions", "trades", "hhi", "orders", "quote", "oi", "oi_daily"]
        },
        "columns": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "rows": {
          "type": "array",
          "items": {
            "type": "array",
            "items": { "type": ["number", "string", "null"] }
          },
          "description": "Each row has the same length and column order as `columns`."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "kind": { "const": "candles" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "time" }],
                "items": { "enum": ["open", "high", "low", "close", "volume"] }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "positions" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "time" }],
                "items": {
                  "enum": [
                    "long", "short", "position", "openinterest",
                    "fiz_long", "fiz_short", "yur_long", "yur_short", "fiz_position", "yur_position"
                  ]
                }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "trades" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "time" }],
                "items": {
                  "enum": ["trades", "trades_b", "trades_s", "vol", "vol_b", "vol_s", "pr_vwap_b", "pr_vwap_s"]
                }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "hhi" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "time" }],
                "items": {
                  "enum": [
                    "hhi_volume", "hhi_buy", "hhi_sell",
                    "hhi_agressive", "hhi_agressive_buy", "hhi_agressive_sell",
                    "hhi_passive", "hhi_passive_buy", "hhi_passive_sell",
                    "hhi_netflow_buy", "hhi_netflow_sell"
                  ]
                }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "orders" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "time" }],
                "items": { "enum": ["bid", "offer", "delta"] }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "quote" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "instrument_key" }],
                "items": {
                  "enum": [
                    "last", "close", "percent", "open", "high", "low",
                    "bid", "offer", "lots", "volume", "numtrades", "updated_at"
                  ]
                }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "oi" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "instrument_key" }],
                "items": {
                  "enum": [
                    "time", "long", "short", "position", "openinterest",
                    "fiz_long", "fiz_short", "yur_long", "yur_short", "fiz_position", "yur_position"
                  ]
                }
              }
            }
          }
        },
        {
          "if": { "properties": { "kind": { "const": "oi_daily" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "columns": {
                "prefixItems": [{ "const": "instrument_key" }],
                "items": {
                  "enum": [
                    "time", "long", "short", "position", "openinterest",
                    "fiz_long", "fiz_short", "yur_long", "yur_short", "fiz_position", "yur_position"
                  ]
                }
              }
            }
          }
        }
      ]
    },

    "period": {
      "enum": ["1min", "5min", "10min", "15min", "30min", "1h", "2h", "4h", "1d"]
    },

    "seriesTableEntry": {
      "title": "One `series.tables[]` element: `#/$defs/table` restricted to the series kinds.",
      "allOf": [
        { "$ref": "#/$defs/table" },
        { "properties": { "kind": { "enum": ["candles", "positions", "trades", "hhi", "orders"] } } }
      ]
    },

    "snapshotTableEntry": {
      "title": "One `snapshot.tables[]` element: `#/$defs/table` restricted to the snapshot kinds.",
      "allOf": [
        { "$ref": "#/$defs/table" },
        { "properties": { "kind": { "enum": ["quote", "oi", "oi_daily"] } } }
      ]
    },

    "series": {
      "type": "object",
      "title": "afbws.market.series.v1 — one instrument's candles or one background dataset.",
      "description": "Reply to `get` (target=series) when `request_id` is present — `mode:\"replace\"`, the full requested range. Unsolicited push to the connection's live series subscription (see `get`'s description for how a request becomes that subscription) when `request_id` is absent — `mode:\"merge\"`: `tables` carries rows only at the timestamp(s) of the latest candle(s) (the just-closed bar plus the new one, on a period rollover), for every `kind` of the live subscription, not the whole history.",
      "required": ["channel", "schema", "instrument_key", "period", "tz", "received_at", "mode", "tables"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.series.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "instrument_key": { "$ref": "common.v1.json#/$defs/instrumentKey" },
        "period": { "$ref": "#/$defs/period" },
        "tz": {
          "type": "string",
          "minLength": 1,
          "description": "IANA timezone of the trading venue (e.g. \"Europe/Moscow\") that bar boundaries and trading-day edges are aligned to. Not the client's local timezone."
        },
        "received_at": {
          "type": "string",
          "format": "date-time",
          "description": "When AFB received this data from its source (broker/cache), ISO-8601 with offset. Distinct from the row-level `time` (UTC unix seconds), which is when the bar/event itself occurred."
        },
        "mode": {
          "enum": ["replace", "merge"],
          "description": "\"replace\": this is the full answer to a `get` — the client replaces its local copy of this instrument+period+kind range with `tables`. \"merge\": this is a live push to the connection's series subscription — `tables` rows are only at the latest candle time(s) (closed bar + new bar on a period rollover); the client upserts them by `time` into its existing copy."
        },
        "from": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Start of the range actually covered by `tables`, YYYY-MM-DD in `tz`."
        },
        "to": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "End of the range actually covered by `tables`, YYYY-MM-DD in `tz`."
        },
        "source": {
          "enum": ["broker", "cache"],
          "description": "Where this data came from, when known."
        },
        "message": {
          "type": "string",
          "description": "Optional human-readable note (e.g. partial coverage, provider fallback)."
        },
        "tables": {
          "type": "array",
          "items": { "$ref": "#/$defs/seriesTableEntry" }
        }
      }
    },

    "snapshot": {
      "type": "object",
      "title": "afbws.market.snapshot.v1 — cross-instrument slice (quote / oi / oi_daily).",
      "description": "Reply to `get` (target=snapshot) when `request_id` is present; unsolicited push to the `quotes` or `futures` subscription scope when it is absent (see `scope`).",
      "required": ["channel", "schema", "scope", "tz", "received_at", "mode", "tables"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.snapshot.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "scope": {
          "enum": ["quotes", "futures", "get"],
          "description": "Which client-side cache this snapshot replaces/updates. \"quotes\": the favorites subscription (accepted `subscribe.quotes`) — `mode:\"full\"` right after `subscribe`, then `mode:\"update\"` with only the quote rows that changed. \"futures\": the futures screener scope (accepted `subscribe.futures`) — always `mode:\"full\"`, `tables` covering quote + oi + oi_daily for every futures instrument; sent right after `subscribe` and again on every futures-positions (futoi) refresh from the broker (~5 min). \"get\": the reply to a one-shot `get` (target=snapshot) — carries `request_id`."
        },
        "tz": {
          "type": "string",
          "minLength": 1,
          "description": "IANA timezone of the trading venue, used to align `oi_daily`'s midnight `time` to the trading day."
        },
        "received_at": {
          "type": "string",
          "format": "date-time",
          "description": "When AFB received this data from its source, ISO-8601 with offset."
        },
        "mode": {
          "enum": ["full", "update"],
          "description": "\"full\": the complete answer to a `get`, or a full refresh of a subscription scope — the client replaces its local snapshot for that `scope`. \"update\": a live push carrying only the rows that changed since the last full/update — see `scope` for which scopes ever send `update`."
        },
        "tables": {
          "type": "array",
          "items": { "$ref": "#/$defs/snapshotTableEntry" }
        }
      }
    },

    "calendarWindow": {
      "type": "object",
      "title": "One resolved trading-session window for the requested instrument.",
      "required": ["start", "end", "phase", "estimated"],
      "additionalProperties": false,
      "properties": {
        "start": {
          "type": "integer",
          "description": "Window start, UTC unix seconds (already resolved for this instrument's board — see afb2bf payloads/calendar.update.json for the raw board->section resolution AFB performs before answering; AFB does not resolve by secid on this path, see `calendar`'s own description)."
        },
        "end": {
          "type": "integer",
          "description": "Window end, UTC unix seconds. Inclusive of the last tradable second."
        },
        "phase": {
          "type": "string",
          "description": "Continuous-trading session phase label, e.g. \"morning\", \"main\", \"evening\", \"weekend\", \"closing_price\". Free-form, not a closed enum — see payloads/calendar.update.json#/properties/sections/items/properties/windows/items/properties/phase for the full category vocabulary. This array never carries an AUCTION-category phase (\"opening_auction\"/\"closing_auction\") — see `calendar`'s own description: no trades happen during an auction, so it is not a chart bar window."
        },
        "estimated": {
          "type": "boolean",
          "description": "True when this window was projected from a learned/default template rather than read from the exchange's official same-day session schedule."
        }
      }
    },

    "calendar": {
      "type": "object",
      "title": "afbws.market.calendar.v1 — trading calendar/session windows and daily-bar dates for one instrument.",
      "description": "Reply to `get` (target=calendar); always carries `request_id` — this is a one-shot lookup, there is no live calendar subscription/push. Built by AFB from the same trading-calendar state it pushes to BF via afb2bf `calendar.update` (payloads/calendar.update.json), resolved down to the single requested `instrument_key`'s board (AFB does not resolve by secid on this path — see payloads/calendar.update.json#/properties/sections/items/properties/windows for why the AFB-internal template lookup dropped that level) and converted to UTC unix seconds for direct use by the chart. `windows` here contains ONLY continuous-trading phases (auction-category windows are filtered out — a chart bar cannot form during an auction, no trades happen) — contrast with `calendar.update` pushed to BF, which carries both categories since BF's entry gate treats an auction as open too. Used by the frontend to render future (whitespace) bar slots beyond the last real candle.",
      "required": ["channel", "schema", "instrument_key", "tz", "received_at", "from", "till", "section", "horizon_until", "windows", "daily_bars"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.calendar.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "instrument_key": { "$ref": "common.v1.json#/$defs/instrumentKey" },
        "tz": {
          "type": "string",
          "minLength": 1,
          "description": "IANA timezone of the trading venue (e.g. \"Europe/Moscow\") that `daily_bars` day boundaries are aligned to. Not the client's local timezone."
        },
        "received_at": {
          "type": "string",
          "format": "date-time",
          "description": "When AFB assembled this answer from its trading-calendar state, ISO-8601 with offset."
        },
        "from": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Start of the requested range, YYYY-MM-DD in `tz` — echoes the `get` request's `from`."
        },
        "till": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "End of the requested range, YYYY-MM-DD in `tz` — echoes the `get` request's `till`, clamped to `horizon_until` when the request asked for more than AFB's calendar snapshot currently covers."
        },
        "section": {
          "enum": ["stock", "futures", "currency"],
          "description": "MOEX trading section this instrument resolved to (stock includes indices) — same vocabulary as payloads/calendar.update.json#/properties/sections/items/properties/section."
        },
        "horizon_until": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Last date AFB's underlying calendar snapshot actually covers (see payloads/calendar.update.json#/properties/horizon_until) — may be earlier than the requested `till` when the snapshot's horizon is shorter than the request."
        },
        "windows": {
          "type": "array",
          "items": { "$ref": "#/$defs/calendarWindow" },
          "description": "Trading-session windows for this instrument covering `[from, till]`, in chronological order."
        },
        "daily_bars": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
          },
          "description": "Dates (YYYY-MM-DD in `tz`) a `1d` candle exists/will exist on, per the section's daily-bar rule: for `stock` (and indices), every traded day including weekend (`W`) sessions; for `futures`, the unique set of `trade_session_date` values (a weekend `W` day's activity folds into its following trading day's bar, so the weekend date itself is not listed separately); for `currency`, trading weekdays only. Used by the frontend together with `windows` to place future whitespace bar slots on the chart."
        }
      }
    },

    "subscriptionQuotesSpec": {
      "type": "object",
      "title": "`quotes` (favorites) subscription scope, as requested by the client or echoed back as accepted.",
      "required": ["instrument_keys"],
      "additionalProperties": false,
      "properties": {
        "instrument_keys": {
          "type": "array",
          "maxItems": 2000,
          "uniqueItems": true,
          "items": { "$ref": "common.v1.json#/$defs/instrumentKey" }
        }
      }
    },

    "subscribe": {
      "type": "object",
      "title": "afbws.market.subscribe.v1 — replace-all subscription statement (client -> server).",
      "description": "The client states its whole desired subscription scope every time; the server does not diff against a previous `subscribe`. `quotes` is the favorites price-plaque scope: `instrument_keys` to track. `futures` opts into the futures screener scope: a full quote+oi+oi_daily snapshot of every futures instrument (no per-instrument list — it's all-or-nothing). Neither controls the series (candles/dataset) subscription — that one is driven entirely by `get` (target=series), see its description. An empty body — neither `quotes` nor `futures` present — unsubscribes from both. Reply is `subscription` with the same `request_id`, immediately followed by `snapshot` push(es) with `mode:\"full\"` for each accepted scope.",
      "required": ["channel", "schema", "request_id"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.subscribe.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "quotes": { "$ref": "#/$defs/subscriptionQuotesSpec" },
        "futures": {
          "type": "boolean",
          "description": "True to subscribe to the futures screener scope (all futures instruments); omitted or false leaves/unsubscribes it."
        }
      }
    },

    "subscriptionRejection": {
      "type": "object",
      "required": ["instrument_key", "code"],
      "additionalProperties": false,
      "properties": {
        "instrument_key": { "$ref": "common.v1.json#/$defs/instrumentKey" },
        "code": { "$ref": "common.v1.json#/$defs/errorCode" },
        "message": { "type": "string" }
      }
    },

    "subscription": {
      "type": "object",
      "title": "afbws.market.subscription.v1 — accepted subscription state, reply to subscribe.v1.",
      "description": "`quotes` is the accepted subset of what was requested (same shape as subscribe.v1's `quotes`; absent means the quotes scope is empty/unsubscribed). `futures` always reflects the accepted state (true/false), even when the request omitted it. `rejected` lists `quotes.instrument_keys` that could not be resolved/subscribed, with a reason code. Always followed by `snapshot` push(es) with `mode:\"full\"` for each accepted scope.",
      "required": ["channel", "schema", "request_id", "futures", "rejected"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.subscription.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "quotes": { "$ref": "#/$defs/subscriptionQuotesSpec" },
        "futures": { "type": "boolean" },
        "rejected": {
          "type": "array",
          "items": { "$ref": "#/$defs/subscriptionRejection" }
        }
      }
    },

    "get": {
      "type": "object",
      "title": "afbws.market.get.v1 — one-shot request (client -> server).",
      "description": "Reply is `series` (target=series), `snapshot` (target=snapshot) or `calendar` (target=calendar) with the same `request_id`, or `error`. For target=series, `get` doubles as the subscription request: a connection has at most one live series subscription (instrument_key, period, kinds), there is no separate subscribe message for it. If `end_date` is absent, or not earlier than \"today\" in the instrument's market `tz`, this request ALSO becomes that live subscription, replacing whatever the connection was previously subscribed to — the server then pushes `series` (mode:\"merge\") for it as new data arrives. A target=series request with `end_date` strictly before today (history paging, e.g. scrolling a chart back) is a pure history fetch and leaves the live subscription untouched. target=calendar is a plain one-shot lookup (no subscription, no push) of trading-session windows and daily-bar dates for one instrument over `[from, till]`.",
      "required": ["channel", "schema", "request_id", "target"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.get.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "target": { "enum": ["series", "snapshot", "calendar"] },
        "instrument_key": { "$ref": "common.v1.json#/$defs/instrumentKey" },
        "period": { "$ref": "#/$defs/period" },
        "kinds": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string" }
        },
        "start_date": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "target=series only: first date to fetch, YYYY-MM-DD in the instrument's market tz."
        },
        "end_date": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "target=series only: last date to fetch, YYYY-MM-DD; omitted means up to the latest available. Also decides whether this request becomes the connection's live series subscription — see the parent description."
        },
        "base": {
          "type": "string",
          "description": "target=series only: opaque provider/period hint (candle base), passed through unchanged."
        },
        "instrument_keys": {
          "type": "array",
          "maxItems": 2000,
          "uniqueItems": true,
          "items": { "$ref": "common.v1.json#/$defs/instrumentKey" },
          "description": "target=snapshot only: instruments to fetch; omitted means every instrument with cached data (used by TickersList)."
        },
        "from": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "target=calendar only: first date to cover, YYYY-MM-DD in the instrument's market tz."
        },
        "till": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "target=calendar only: last date to cover, YYYY-MM-DD in the instrument's market tz."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "target": { "const": "series" } }, "required": ["target"] },
          "then": {
            "required": ["instrument_key", "period", "kinds", "start_date"],
            "properties": {
              "kinds": { "items": { "enum": ["candles", "positions", "trades", "hhi", "orders"] } },
              "instrument_keys": false,
              "from": false,
              "till": false
            }
          }
        },
        {
          "if": { "properties": { "target": { "const": "snapshot" } }, "required": ["target"] },
          "then": {
            "required": ["kinds"],
            "properties": {
              "kinds": { "items": { "enum": ["quote", "oi", "oi_daily"] } },
              "instrument_key": false,
              "period": false,
              "start_date": false,
              "end_date": false,
              "base": false,
              "from": false,
              "till": false
            }
          }
        },
        {
          "if": { "properties": { "target": { "const": "calendar" } }, "required": ["target"] },
          "then": {
            "required": ["instrument_key", "from", "till"],
            "properties": {
              "period": false,
              "kinds": false,
              "start_date": false,
              "end_date": false,
              "base": false,
              "instrument_keys": false
            }
          }
        }
      ]
    },

    "error": {
      "type": "object",
      "title": "afbws.market.error.v1",
      "required": ["channel", "schema", "code", "message"],
      "additionalProperties": false,
      "properties": {
        "channel": { "const": "market" },
        "schema": { "const": "afbws.market.error.v1" },
        "request_id": { "$ref": "common.v1.json#/$defs/requestId" },
        "code": { "$ref": "common.v1.json#/$defs/errorCode" },
        "message": { "type": "string", "minLength": 1 },
        "details": { "type": "object" }
      }
    }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rolo837/AFB-BF-protocol/spec/schemas/afbws/account.snapshot.v1.json",
  "title": "account/get_account response + account push (AFB-only; not part of afb.execution.v1 wire)",
  "description": "Pure proxy of BF's `broker.account` payload (see belphegor/proxy.py: `dict(envelope.payload)`) plus the AFB envelope wrapper. `data` is intentionally untyped — BF owns that shape and it isn't schematized anywhere (no afb.execution.v1 schema for broker.account); the frontend normalizes it defensively (see belphegor.ts normalizeCashBalances/normalizePositions) rather than trusting a strict shape.",
  "type": "object",
  "required": ["type", "bf_id", "data"],
  "additionalProperties": false,
  "properties": {
    "type": { "const": "account" },
    "bf_id": { "type": "string" },
    "data": { "type": "object", "additionalProperties": true },
    "revision": { "type": "integer", "description": "Merged in by event_translator._merge_snapshot_meta when present on the BF payload." },
    "as_of": { "type": "string" },
    "source": { "type": "string" }
  }
}

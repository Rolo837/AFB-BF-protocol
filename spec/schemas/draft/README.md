# Draft schemas (not part of the published package surface)

Parked drafts formerly published as `spec/schemas/settings/` in the retracted
`v2.0.2` attempt to type the AFB frontend↔backend `settings` WS channel and
`config/users/*.yaml`.

These files are **not** wired into `afb-bf-protocol-generate` model exports
(`ts/src/models.ts` / `models_generated.py`) and are **not** mirrored into the
packaged `python/afb_bf_protocol/schemas/` tree. Keep them here only as a
reference until a real consumer path exists.

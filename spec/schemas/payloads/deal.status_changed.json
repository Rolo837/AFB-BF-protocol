{
  "type": "object",
  "properties": {
    "at": {
      "type": "string",
      "description": "ISO-8601 BF event-occurrence time (same value as the BF journal entry `at`)."
    },
    "deal_id": {
      "type": "string"
    },
    "execution_phase": {
      "type": "string"
    },
    "last_price": {
      "type": "string"
    },
    "quantity": {
      "type": "integer",
      "description": "Resolved order quantity (lots), fixed once by BF at entry-trigger time (`resolved_quantity`). Present only once known — never present before the entry order is placed, never cleared afterward."
    },
    "revision": {
      "type": "integer"
    },
    "status": {
      "type": "string"
    }
  },
  "additionalProperties": true,
  "required": [
    "deal_id",
    "revision",
    "status"
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rolo837/AFB-BF-protocol/spec/schemas/payloads/deal.status_changed.json",
  "title": "payload: deal.status_changed"
}

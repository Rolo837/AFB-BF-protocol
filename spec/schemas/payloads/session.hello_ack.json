{
  "type": "object",
  "properties": {
    "server_nonce": {
      "type": "string"
    },
    "heartbeat_interval_sec": {
      "type": "integer",
      "minimum": 1,
      "description": "Negotiated heartbeat interval (seconds): BF proposal from session.hello, clamped by AFB to connector_defaults.heartbeat_interval_min/max. BF must use this value for session.heartbeat period."
    },
    "protocol": {
      "type": "string"
    },
    "accepted_protocol": {
      "type": "string"
    },
    "dry_run": {
      "type": "boolean"
    },
    "dry_run_afb": {
      "type": "boolean",
      "deprecated": true,
      "description": "Deprecated: use dry_run (effective session value) instead."
    },
    "dry_run_bf": {
      "type": "boolean",
      "deprecated": true,
      "description": "Deprecated: use dry_run (effective session value) instead."
    },
    "margin_trading": {
      "type": "boolean"
    },
    "margin_trading_afb": {
      "type": "boolean",
      "deprecated": true,
      "description": "Deprecated: use margin_trading (effective session value) instead."
    },
    "margin_trading_bf": {
      "type": "boolean",
      "deprecated": true,
      "description": "Deprecated: use margin_trading (effective session value) instead."
    },
    "features": {
      "type": "object",
      "description": "AFB-side capability flags for this session — read by BF's afb_client (see afb_client/ws_client.py, alongside dry_run/margin_trading above) to decide what it may send this AFB.",
      "properties": {
        "multi_account": {
          "type": "boolean",
          "description": "This AFB build understands broker.accounts (see payloads/broker.accounts.json) and negotiated_support for it. When true, BF sends broker.accounts (in response to broker.get_accounts, and unsolicited after every reconcile pass) and stops sending the deprecated broker.account to this session. Absent/false means BF must keep sending broker.account only."
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true,
  "required": [
    "protocol"
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Rolo837/AFB-BF-protocol/spec/schemas/payloads/session.hello_ack.json",
  "title": "payload: session.hello_ack"
}
